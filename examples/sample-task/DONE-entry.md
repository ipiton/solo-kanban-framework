# Sample `DONE.md` Entry

This is the compact closure line that landed in the project's `DONE.md` when the task closed.

```markdown
## 2026-04-13

- **USERS-ME-LAST-LOGIN** — Add `last_login_at` to `GET /users/me`. Standard tier (`C R`). Additive field on existing endpoint, no migration. Frontend FE-2104 unblocked. Spec/Tests/OpenAPI all updated. Archive: `tasks/archive/users-me-last-login/`.
```

## Notes

- The entry stays under one paragraph by design — full evidence lives in the archived workspace.
- Tier (`Standard`) and signals (`C R`) are recorded inline so future readers can scan the log by risk profile without reading every spec.
- The archive path is the durable link to the full record.
